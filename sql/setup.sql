DROP DATABASE IF EXISTS `archery`;
CREATE DATABASE `archery`;

-- Add the table definitions here. Best bet is to create them using MySQL Workbench (or similar) and then export the schema, replacing this file.
